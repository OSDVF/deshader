/// Handles to objects created in the backend API.
/// For example: GL_BUFFER or GL_TEXTURE
pub const PlatformRef = u64; // 64bit because Vulkan always uses 64bit handles
pub const Stage = opaque {};
